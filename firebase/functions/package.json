{
  "name": "towing-dispatch-functions",
  "description": "Cloud Functions for the Towing Dispatch System. Firebase CLI commands must be run from the firebase/ directory (where firebase.json lives), not from this directory.",
  "version": "1.0.0",
  "engines": {
    "node": "22"
  },
  "main": "src/index.js",
  "scripts": {
    "shell": "firebase functions:shell",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2"
  },
  "private": true
}
